# Codex Master Prompt — 尸潮乱斗 / Horde Mayhem

你现在接手的是一个已经可以运行的 Three.js 前端僵尸 FPS 原型。当前源码就在本仓库中。你的任务是“大改和扩展”，但必须基于现有项目继续做，不能为了重写而重写，也不能擅自删掉、替换、弱化或改变下面任何已确认需求。

## 0. 最高优先级规则

1. **先完整检查现有项目，再修改。** 先阅读 `public/index.html`、`public/app.html`、现有 CSS、`public/js/home.js`、`settings.js`、`game.js`、`audio.js`，确认当前能运行的功能、UI、Three.js 场景、输入、音频、FPS、暂停、HUD 与设置保存方式。
2. **保留当前能工作的效果。** 新实现必须是增量升级。除非本提示词明确要求改变，否则不要改变现有手感、颜色、按钮逻辑、镜头行为、枪械反馈或页面流程。
3. **禁止“看起来差不多”的替代。** 所有要求都要落成实际可运行的代码，不能只做静态 UI、假按钮、假联机、假登录、假在线状态或纯文字占位。
4. **不要过度拆文件。** 允许重构，但文件数量保持合理。核心逻辑按“UI / Game / Network / Audio / Config”这样的粒度组织，不要把每个小功能拆成独立文件。
5. **所有新增功能必须兼容桌面、平板、手机。** 不能只在鼠标键盘上可用。
6. **游戏必须双语。** 中文名固定为 **《尸潮乱斗》**，英文名固定为 **Horde Mayhem**。中文和英文使用同一套页面和逻辑，通过 i18n 文本表切换，不要复制两套 HTML。
7. 如果某个数值（经济比例、升级经验、伤害、Boss 技能数值等）没有在本需求中明确，不要擅自把它写死成无法调整的最终值。统一放到配置对象中，并使用合理的初始值，方便后续平衡。
8. 每完成一个主要模块都要实际运行检查，修复控制台错误、资源 404、模块路径错误、移动端触摸冲突和 WebSocket 重连问题。

---

## 1. 产品定位

游戏名称：**尸潮乱斗 / Horde Mayhem**。

类型：双语 FPS 僵尸生存游戏，支持 **单人** 与 **多人组队**。

核心体验：玩家在破败城市环境中使用枪械对抗不断出现的僵尸潮和 Boss。战斗重点是射击反馈、移动体验、波次压力、合作、成长和奖励，而不是阴暗到看不清场景的恐怖风格。

视觉方向：现代枪击游戏 UI + 破败城市 + 清晰可见的环境。可以有危险、末日和紧张氛围，但整体不能长期处于“黑到看不清”的状态。

---

## 2. 建议项目结构：重新整理，但不要太散

基于当前项目重构为接近下面的结构；如果现有文件能合并复用，就复用，不要机械增加文件。

```text
/
├─ public/
│  ├─ index.html              # 登录 + 主界面
│  ├─ app.html                # 实际游戏
│  └─ assets/
│     ├─ audio/
│     ├─ textures/
│     ├─ models/
│     └─ icons/
├─ src/
│  ├─ css/
│  │  ├─ main.css             # 登录、主页、商城、军火处、设置
│  │  └─ game.css             # HUD、Loading、移动端控制、暂停界面
│  └─ js/
│     ├─ ui.js                # 登录后主页、菜单、商城、军火处、设置、i18n
│     ├─ game.js              # Three.js、玩家、武器、僵尸、Boss、场景、碰撞、波次
│     ├─ network.js           # 登录 API、好友、邀请、房间、WebSocket、延迟
│     ├─ audio.js             # BGM、枪声、僵尸空间音效、命中反馈
│     └─ config.js            # FPS、难度、武器、经验、奖励、平衡参数、文本 key
├─ worker/
│  ├─ worker.js               # Cloudflare 后端入口/API/WebSocket
│  └─ schema.sql              # 用户/好友/背包/进度/房间等数据表
├─ package.json
├─ wrangler.toml
└─ README.md
```

目标是“清楚但不碎”。如果没有必要，不要继续拆成几十个 JS 文件。

GitHub 用作源码仓库和版本管理。Cloudflare 负责部署。前端可由 Cloudflare Pages/静态部署承载；登录、玩家数据、好友系统、房间与联机由 Cloudflare Worker 处理。实时房间状态优先使用 WebSocket；需要房间级状态协调时使用适合 Cloudflare 的有状态方案（例如 Durable Objects）。持久数据使用 Cloudflare 可用的数据库方案（例如 D1）。不要把真实用户数据只存在 localStorage。

---

## 3. 登录系统

在现有 `index.html` 基础上加入完整登录流程，但主页仍然是同一个入口，不要增加一堆页面。

需要：注册、登录、退出登录、保持登录状态、用户名/显示名、基础错误提示、Loading、断线/服务器不可用提示。

登录成功后进入主页。未登录用户不能进入在线多人功能；是否允许离线单人试玩，可以保留成配置开关，但默认流程以登录后主页为主。

安全要求：密码绝不能明文保存；不要把密钥写进前端；后端进行验证；前端只保存安全的会话信息。所有登录/玩家资料接口都要有失败处理。

---

## 4. 主页信息架构

主页必须保持游戏化 UI，不要做成普通后台网页。

主页主要入口固定为：

- **开始游戏 / Play** → 选择模式 → **单人 / Solo** 或 **多人 / Multiplayer**
- **军火处 / Armory** → 管理、查看、升级和装备枪械
- **黑市 / Black Market** → 商城式购买枪械与物品
- **设置 / Settings**

同时显示玩家基础信息，例如昵称、个人等级、货币，但不能占据大量视野。

黑市必须有真正的商城视觉：卡片、枪械缩略图/3D 或高质量图标、价格、状态、购买按钮、已拥有状态、锁定条件。禁止只做一行行纯文字菜单。

军火处需要显示枪械的模型/图标、等级、武器 EXP、关键数据和装备槽。玩家可以同时携带 **手枪 + 步枪**。

默认枪械：**手枪**。

当前第一把可购买/解锁步枪：**AK-47**。现阶段只需要把 AK-47 做完整，不要擅自加入大量其他枪械。

---

## 5. 单人 / 多人模式

### 单人

直接创建本地/服务端认可的单人战局。单人有独立的难度平衡和奖励表。

### 多人

多人是“创建房间 + 好友组队”。玩家创建房间后，可以邀请好友进入。房间成员准备后进入游戏。

好友系统要求：

- 只有添加为好友后，才能查看该玩家的在线 / 离线状态。
- 好友可以发送组队邀请。
- 被邀请玩家必须看到清晰但不遮挡游戏关键视野的邀请栏。
- 邀请包含接受、拒绝、倒计时/过期状态。
- **邀请发送必须有 15 秒冷却时间**，避免刷屏和骚扰。
- 冷却需要由后端同时校验，不能只做前端按钮禁用。
- 同一发送者对同一玩家已有有效邀请时应去重，避免重复堆叠。
- 游戏中收到邀请时，UI 要尽量轻量，不抢占准星和战斗中心区域。

多人房间内同步至少包括：房间成员、准备状态、玩家进入/离开、关键游戏状态和延迟信息。不要把多人做成只在 UI 上显示几个假玩家。

---

## 6. 难度系统

固定提供：

- 简单 / Easy
- 普通 / Normal
- 困难 / Hard
- 噩梦 / Nightmare
- 梦魇 / Dreadmare

原则：难度越高，风险越高，最终奖励越高。

**单人和多人必须使用不同的难度平衡参数。** 不允许多人只是把单人僵尸数量简单乘人数。分别配置生命、伤害、刷新速度、数量上限、Boss 强度、奖励倍率等，使多人合作不会因为人数增加而失衡。

所有参数集中放入 `config.js` 或等价配置层，不要散落硬编码。

---

## 7. FPS 与性能

FPS 设置固定提供：**30 / 60 / 90 / 120 / 144**。

首次进入时需要根据设备性能和浏览器实际渲染能力做一次轻量检测，并选择合理默认值。不要仅通过 User-Agent 判断。

建议策略：

- 性能较低或移动端明显吃力：30 FPS
- 普通手机/普通电脑：60 FPS
- 高刷新率且稳定设备：90 FPS
- 120Hz 级设备并且性能稳定：120 FPS
- 144Hz+ 桌面设备并且性能稳定：144 FPS

需要通过短时间 `requestAnimationFrame` 采样和基础性能观察估算显示/渲染能力。用户手动选择后优先尊重用户设置，并持久保存。

不能为了追求高 FPS 导致设备持续掉帧和发热。可以动态降低阴影、粒子、远处细节或渲染分辨率，但不能突然改变游戏核心视觉风格。

HUD 显示 **实时 FPS + 网络延迟 Ping**，位置放在边缘，尺寸小、对比清晰，不遮挡准星、血量、弹药和敌人。

---

## 8. 桌面 / 平板 / 手机控制

### 桌面

保留鼠标键盘 FPS 操作：WASD 移动、鼠标视角、射击、换弹、跳跃、ESC 暂停等。

### 手机和平板

移动端必须是认真设计的 FPS 控制，不是简单加几个 HTML 按钮。

左下：**动态移动摇杆**。触点落下时可以在合理区域生成摇杆中心，带死区、最大半径、平滑输入和回弹。摇杆不能挡住大量画面，并适配安全区域刘海/圆角。

右半屏：滑动控制镜头，支持多点触控，不能与右侧动作按钮冲突。

右下核心按钮：射击。附近布局跳跃、换弹、瞄准等按钮，按钮大小必须适合拇指操作，并根据手机/平板尺寸自适应。

**瞄准 ADS 支持两种方式：长按 / 点击切换，默认是“点击切换”。** 在设置中可切换。

支持手机震动/触觉反馈时：射击、受伤、Boss 重击等可以使用轻量震动，但提供开关，不能连续震到影响体验。

移动端要求：同时移动 + 转向 + 射击 + 跳跃的多指输入不能互相取消。

---

## 9. Three.js 玩家、手部和枪械重构

现有第一人称手部和枪械需要视觉重构，使其更接近枪击游戏：比例自然、姿态合理、枪不遮挡过多视野。

加入：待机呼吸、走路 sway、跑步 bob、开火 recoil、换弹动作、ADS 过渡、落地反馈。动画必须平滑，不能每帧突跳。

枪械和手部必须与 FOV、ADS、移动速度协同。移动端和桌面使用同一套世界逻辑，只调整输入方式。

默认手枪要完整可用；AK-47 解锁后有独立模型/外观、弹匣、射速、伤害、后坐力和武器 EXP。

---

## 10. 僵尸与 Boss

普通僵尸不能像简单方块直接平移追踪。需要更生动：身体朝向、步行动作、受击反应、死亡反馈、轻微个体差异、攻击动作和更自然的追踪速度变化。

优先优化“看起来像在移动和攻击”，不要只堆高多边形。

Boss：

- 体型明显大于普通僵尸。
- 有独立视觉辨识度。
- 有多个技能或攻击模式。
- **所有强技能必须有冷却时间。**
- 必须存在清晰的前摇、预警和可躲避窗口。
- 禁止 Boss 连续无间隔释放技能，让玩家无法操作。
- Boss 降临时进入独立阶段并切换音乐。

Boss 和普通怪物参数同样放在配置层，方便后续平衡。

---

## 11. 命中、枪感与反馈

射中身体和射中头部必须明显不同。

身体命中：普通 hit marker、声音、粒子/受击反馈。

头部命中：不同的 hit marker / 音效 / 反馈，并在准星下方一点的位置短暂显示 **“爆头 / HEADSHOT”**。不能长期挡住画面。

射击需要合理的后坐力、枪口火焰、轻微镜头反馈和支持设备的震动反馈。效果要有力量，但不要夸张到看不清敌人。

所有命中判断、头部判定和伤害逻辑必须与 Three.js 碰撞/射线一致，不能只做视觉假反馈。

---

## 12. 音频系统

需要三套主要 BGM 状态：

1. 主页 BGM
2. 普通游戏战斗 BGM
3. Boss 降临后的紧张 / 惊险 BGM

状态切换要有淡入淡出，不能突然截断。

可以寻找网络上的合适音乐和音效，但**只使用明确允许游戏项目使用的免版税、CC0、公共领域或许可清楚的资源**。把来源、作者、许可和 URL 记录到 README 或资源清单。许可不明确时不要直接打包使用，可以先保留资源接口和占位文件。

枪声、身体命中、爆头、换弹、受伤、Boss 技能等需要区分音效。

僵尸声音使用距离衰减和空间方向感：离玩家越远越小，方向与世界位置对应。不要让整个地图所有僵尸以同样音量播放。

---

## 13. 场景与碰撞

场景方向：**破败城市**，不是纯黑暗地牢。

需要道路、损坏建筑、围栏、废车/障碍、路灯或环境光、破损元素等，整体有末日城市感，但玩家能清楚识别道路、敌人和可行动区域。

所有实际看起来应该阻挡玩家的大型物体都需要对应碰撞体。玩家不能穿建筑、墙、车辆、围栏或主要障碍物。

碰撞与视觉几何尽量解耦，优先使用简化碰撞体保障性能。

僵尸也不能毫无规则穿过主要墙体。可以使用简单路径绕行/导航逻辑，不要求一开始做重型导航系统，但必须明显减少穿墙。

---

## 14. Loading 与适配

进入主页和游戏时都需要有完整 Loading 体验。

Loading 至少显示：游戏 Logo/名称、资源加载进度、当前加载阶段；不能假装 0→100。如果模型/纹理/音频有真实加载任务，进度要根据真实任务推进。

页面必须适配：桌面、平板、手机、横屏。游戏移动端以横屏体验优先；竖屏进入游戏时给清晰提示。

HUD 在所有尺寸上不能挡住主要视野。

---

## 15. 结算、积分、货币与成长

每局产生战斗积分。**游戏结算后，积分根据结算规则转换为游戏货币**。难度越高，奖励倍率越高。

货币用于黑市购买枪械和物品。当前至少支持购买/解锁 AK-47。

武器升级不是直接用玩家等级替代。每一把武器都有独立 **Weapon EXP**，战局结束后根据该武器本局实际使用/表现获得经验，达到条件后升级。

同时存在玩家个人 **Player Level**，它有独立经验和升级条件。

武器满级应该是长期目标，不能几局就满。具体经验曲线、等级上限和奖励数值全部配置化，不要散落在逻辑代码中。

服务器上线后，货币、购买记录、武器等级、武器 EXP、玩家等级必须以后端数据为准，前端 localStorage 只能缓存显示或本地设置，不能作为最终权威数据，避免玩家直接修改浏览器数据作弊。

---

## 16. 暂停与游戏内菜单

保留现有 ESC 暂停体验。暂停菜单至少包含：

- 继续游戏
- 设置
- 退出当前战局

单人可以真正暂停世界时间；多人模式中打开 ESC 只能打开菜单，服务器战局不能因为某个玩家暂停而停止。

移动端提供不显眼但容易点击的暂停按钮。

---

## 17. 联机与延迟体验

多人优先保证“能玩”和“稳定”，不要一开始做复杂竞技级网络代码。

需要：WebSocket 连接、房间加入/离开、重连处理、Ping 计算、断线提示、玩家状态同步、基础敌人/波次同步、结算一致性。

不要让客户端完全决定货币、经验、购买结果、房间奖励或关键伤害结算。关键结果需要服务端校验/确认。

网络暂时不稳定时，UI 只做小型提示，避免不停弹大型 Modal 干扰战斗。

---

## 18. 双语

所有用户可见文本必须走统一 i18n 表，包括：登录、主页、模式、房间、好友、邀请、难度、军火处、黑市、设置、Loading、HUD、Boss、结算、错误提示。

默认语言可根据浏览器语言决定，但玩家可以在设置中手动切换中文 / English，并保存选择。

游戏名显示：

- 中文：尸潮乱斗
- English: Horde Mayhem

不要翻译或改掉中文正式名称。

---

## 19. 实现顺序

不要一次把全部系统胡乱塞进一个大文件。按下面顺序推进，同时保证每阶段可以运行：

**Phase 1 — Audit & Preserve**：检查现有项目，列出当前功能和必须保留的行为，修复已有明显错误但不改变效果。

**Phase 2 — Structure & UI**：重构目录但不拆太散；完成双语、登录 UI、主页、模式选择、军火处、黑市、设置、Loading 与响应式布局。

**Phase 3 — Core Gameplay Upgrade**：Three.js 场景、破败城市、碰撞、玩家手/枪、手枪、AK-47、僵尸、Boss、音效、移动端控制、FPS 自适应和 HUD。

**Phase 4 — Progression**：结算、货币、武器 EXP、玩家等级、购买、装备、数据配置化。

**Phase 5 — Cloudflare Backend**：登录、持久数据、好友、在线状态、邀请 15 秒冷却、房间、服务端校验。

**Phase 6 — Multiplayer**：WebSocket 房间同步、组队战斗、多人专用难度平衡、断线重连、Ping。

每个 Phase 完成后都必须保证主流程仍能运行，不允许为了后续阶段把现有游戏先弄坏。

---

## 20. 验收标准

最终结果至少满足以下事实，不接受仅有界面没有逻辑：

- 《尸潮乱斗 / Horde Mayhem》双语可切换。
- 可以注册/登录，登录后进入主页。
- 主页有开始游戏、军火处、黑市、设置。
- 可以选择单人/多人。
- 多人可以创建房间、查看好友在线状态、邀请好友；邀请有后端 15 秒冷却。
- 五档难度完整，且单人/多人平衡表不同。
- 桌面/平板/手机都能操作；移动端摇杆和多点触控实际可玩。
- ADS 可选长按或点击，默认点击。
- FPS 有 30/60/90/120/144，首次根据设备能力合理选择。
- HUD 有实时 FPS 和 Ping，且不挡视野。
- 场景是清晰的破败城市，有真实碰撞，玩家不能穿墙。
- 普通僵尸移动/攻击更生动。
- Boss 体型更大、有技能、有冷却、有预警、有可躲避窗口。
- 身体命中和爆头反馈不同，爆头文字位于准星下方一点并短暂显示。
- 有主页、游戏、Boss 三种音乐状态；僵尸声音有距离衰减。
- 默认手枪可用；AK-47 可以购买/解锁、装备、获得独立 Weapon EXP 和升级。
- 结算积分可以转成货币；难度越高奖励越高。
- 玩家个人等级独立存在。
- ESC 可以继续、设置、退出；多人打开菜单不会暂停整个服务器。
- 代码无明显控制台错误、无主要资源 404，关键流程有错误处理。

---

## 21. 最重要的“不要做”

不要把游戏改成完全不同的视觉项目；不要删掉原来的可玩 FPS 核心；不要把联机做成纯前端假数据；不要把所有内容塞在一个 `game.js` 里，也不要过度拆成几十个文件；不要使用版权来源不明的音乐直接打包；不要因为 Boss 技能而让玩家连续无法行动；不要让移动端按钮挡住半个屏幕；不要让高 FPS 选项强制所有设备运行；不要让玩家通过改 localStorage 就能获得货币、AK-47、等级或多人奖励。

开始执行时，先输出你对现有代码的检查结果和重构计划，然后再进行修改。修改过程中优先复用现有可用逻辑，并在 README 中说明本地运行方式、GitHub 仓库结构、Cloudflare 部署步骤、所需绑定/数据库初始化方式以及音频/模型资源许可。

---

## 22. 本轮追加要求：登录完善、新手引导、可见手部动作与 Three.js 细节

> **本节是对前面所有需求的追加，不替换、不删除、不弱化前文任何内容。** 如果实现时出现歧义，以“保持现有可玩效果 + 满足前文 + 同时满足本节”为准。不要借本节名义重新设计成另一款游戏。

### 22.1 第一人称手部、枪械与换弹动作必须真正可见

玩家在第一人称中必须始终能看到合理的手部与当前武器，不允许只显示枪、浮空枪、简单方块手臂或只靠镜头抖动模拟动作。

手部与武器至少覆盖以下可见状态，并通过统一动画状态机平滑切换：

- Idle：自然持枪与轻微呼吸，不要夸张晃动。
- Walk：轻微 sway / bob，与移动速度同步。
- Sprint：枪械自然下压或偏移到冲刺姿态，停止冲刺后平滑回到持枪状态。
- ADS：双手和武器一起进入瞄准姿态，不能只有 FOV 突然缩放。
- Fire：扳机/握持姿态、枪身后坐、枪口反馈与手部 recoil 协同。
- Jump / Fall / Land：手部有轻微惯性和落地反馈，但不能遮挡视野或造成眩晕。
- Equip / Weapon Switch：切换手枪与步枪时要有拿出、放下/切换动作，不能瞬间 teleport 武器。
- Inspect / Readiness：可以有很短的待机检查动作作为视觉细节，但不能自动频繁播放干扰战斗，也不要因此新增无关玩法。
- Reload：必须是完整、可读、与弹药逻辑一致的换弹动作。

#### 手枪换弹

至少表现：左手接近弹匣 → 旧弹匣退出 → 新弹匣进入 → 若空仓需要则执行套筒/释放动作 → 恢复持枪姿态。

#### AK-47 换弹

至少表现：支撑手离开护木 → 旧弹匣拆下 → 新弹匣装入并锁定 → 空仓/需要时执行拉机柄或等价动作 → 恢复射击姿态。

换弹不是纯计时条。**弹药数值改变必须与动画中的关键节点对应**，避免动画还没装上弹匣但 HUD 已经满弹，或动画完成却仍然不能射击。空弹换弹和弹匣仍有子弹的战术换弹可以使用不同流程；如果当前阶段暂不制作两套完整动画，也必须把状态机和配置接口预留清楚，不要用杂乱条件硬编码。

换弹期间的开火、ADS、冲刺、切枪等行为必须有明确规则，不能出现复制弹药、无限弹、动画卡死、枪械突然消失或状态不同步。

### 22.2 其他玩家也必须看到人物、手部与武器动作

多人模式不能只同步一个胶囊体、名字或静止模型。其他玩家从第三人称看到的角色至少需要有：

- 基础身体模型、头部朝向、上半身瞄准方向。
- 双手实际握持当前武器，而不是枪漂浮在角色前方。
- Idle / Walk / Run / Sprint / Jump / Land。
- Aim / ADS 的第三人称对应姿态。
- Fire 与可见后坐/枪口反馈。
- Reload，能看到换弹动作。
- Weapon Switch，能看到手枪与 AK-47 的切换。
- 受击和死亡状态与前文要求保持一致。

第一人称本地玩家使用 **viewmodel（手 + 枪）**；世界中的自己/其他玩家使用 **world model（完整角色 + 武器）**。两者动画可以针对视角分别优化，但动作语义必须一致。

多人同步不要发送每根骨骼每一帧的数据。网络层优先同步：玩家位置/朝向、瞄准角度、当前武器、移动状态、动画状态、动作事件（射击/换弹/切枪/跳跃等）与必要时间戳；远端客户端使用插值和本地 AnimationMixer/状态机表现动作。这样既能让其他玩家看见动作，也避免带宽浪费和抖动。

### 22.3 登录系统要达到“真实可用”，不能只是登录框

在前文登录要求基础上，登录/注册系统必须补全以下细节：

- 新用户注册后由服务端创建持久玩家档案，并生成唯一玩家 ID；显示名与登录凭据分开管理。
- 注册时进行用户名/显示名/密码的前后端校验；重复账号、非法输入、网络失败都要有清楚但不过度暴露安全信息的提示。
- 密码只在服务端进行安全哈希处理，数据库中绝不保存明文密码。
- 登录成功后建立真正的服务端会话；刷新网页后可以安全恢复登录状态。
- 长期登录凭证不要直接以可被任意脚本读取的明文形式保存在 localStorage。优先使用适合 Cloudflare 架构的安全会话方案，例如 `HttpOnly + Secure + SameSite` Cookie 或等价安全实现。
- 退出登录必须使服务端会话失效，而不仅仅是删除前端变量。
- 登录接口需要基础频率限制/防暴力尝试；错误文案不要帮助攻击者枚举账号。
- 玩家资料、货币、等级、武器、好友、教学完成状态等全部以后端持久数据为权威。
- Loading、超时、断网、会话过期、服务端错误都有明确状态，不能卡死在按钮上。
- 如果项目没有配置邮件服务，不要伪造“找回密码邮件已发送”这类假功能；相关功能可留扩展接口，但不能假实现。

建议玩家档案至少预留：`playerId`、`displayName`、`language`、`tutorialCompleted`、`playerLevel`、`currency`、武器拥有/装备/EXP、好友关系、创建时间、最近在线时间。具体数据库字段可根据现有 schema 调整，但不要重复存储同一含义的数据。

### 22.4 每个新玩家必须经过短而完整的新手教学

**每一个新注册玩家第一次进入游戏时，都必须完成新手教学后才开放普通单人/多人主流程。** 这是账号级状态，不允许只靠 localStorage 判断，也不能通过刷新页面绕过。

教学目标是快速建立 FPS 基本操作，不做冗长剧情。目标时长约 **2–4 分钟，硬上限尽量控制在 5 分钟以内**。不能塞大量说明文字，也不要让玩家站着看长动画。

教学场景使用与正式游戏相同的 Three.js 核心系统和输入系统，可以是破败城市中的安全训练区/隔离区，不要另外做一套完全不同的小游戏。

教学根据设备自动显示对应提示：

- 桌面：WASD、鼠标视角、跳跃、射击、ADS、换弹。
- 手机/平板：移动摇杆、右侧视角、射击、跳跃、ADS、换弹，多点触控提示。

教学步骤保持精简，建议按以下顺序：

1. 移动与转动视角。
2. 跳跃和基础移动。
3. 使用默认手枪射击固定/缓慢目标。
4. 打空或触发换弹，必须实际完成一次可见换弹动作。
5. 使用 ADS 完成一次瞄准射击。
6. 对一个训练僵尸完成身体命中与头部命中的反馈体验。
7. 结束教学并进入主页/模式选择。

每一步用短提示、视觉高亮和小目标推进，不要用大面积 Modal 挡画面。完成一个步骤后自动进入下一步；重复失败时可以提供轻量提示，但不要降低成自动替玩家完成。

教学进度在关键步骤服务端保存。玩家中途断线或关闭网页后重新登录，应从合理检查点继续，而不是每次从头开始。教学完成后由服务端写入 `tutorialCompleted`，普通模式才解锁。

已经完成教学的玩家可以在设置或训练入口重新体验教学，但不能再次触发“新玩家首次流程”。不要因为重玩教学重复发放经济奖励；本需求当前不要求教学奖励。

### 22.5 Three.js 第一人称 Viewmodel 设计要更精细

Three.js 实现不能只满足“有模型就行”，需要按 FPS 的视觉层级组织：

- 世界场景与第一人称手/枪 viewmodel 分离管理。可以使用独立渲染层/独立 viewmodel camera 或等价方式，避免枪械轻易穿进世界墙体，同时允许 viewmodel 使用更适合枪械的 FOV。
- 即使 viewmodel 使用独立层，也要检测玩家贴近墙面/大型障碍物的距离；过近时武器自然下压/收回，避免看起来能把整把枪插进墙里。
- 手掌、手指、腕部、前臂与枪械握持位置要符合视觉逻辑。左右手不要穿过枪身，弹匣、套筒、机柄等运动部件尽量有独立节点，方便动画。
- 手枪与 AK-47 使用不同握持姿态、ADS 对齐、reload 动作与 recoil 参数，不能只缩放同一模型假装两把枪。
- 动画使用时间/状态驱动，基于 `deltaTime`，不要依赖固定帧数，否则 30/60/120/144 FPS 下动作速度会不同。
- 动画切换使用 easing / blend；不要直接设置瞬间位置造成跳帧感。
- 如果使用 glTF/骨骼模型，优先使用 `AnimationMixer` 和合理的 action cross-fade；不要每帧重新创建 mixer、clip 或大量临时对象。

### 22.6 Three.js 第三人称玩家模型细节

世界角色模型需要与第一人称逻辑对应，但不必追求昂贵的全身模拟。重点是“远端玩家看起来像人在操作枪”。

- 角色移动方向、身体朝向和瞄准方向区分处理；玩家可以向前移动时轻微看向侧面，不要整个身体瞬间硬转。
- 上半身瞄准可使用骨骼旋转/aim offset 或等价方案，让枪口方向大致对应玩家实际视角。
- 手部 IK 如果当前资产/复杂度允许可以使用；如果不实现完整 IK，也必须保证手掌绑定点和枪械 socket 稳定，不出现手和枪分离。
- 武器使用 socket/attach point 挂接到手部骨骼，不要每帧手工猜坐标。
- 远端位置和旋转用插值，收到网络包时不要硬 teleport；真正的大距离纠正才允许快速拉回。
- 射击、换弹、切枪等动作通过事件触发并与状态机校验，避免网络抖动反复重播同一动画。

### 22.7 Three.js 僵尸、Boss、材质、灯光和场景细节

在前文“破败城市但不能过暗”的方向上进一步细化：

- 环境照明需要有主方向光/环境光或等价组合，让人物、枪械和僵尸轮廓清楚；可以有阴影和危险氛围，但不能靠把曝光压黑制造恐怖感。
- 使用适合 Three.js 的 PBR 材质流程时，正确处理 base color、normal、roughness、metalness 等贴图；纹理色彩空间、过滤和 mipmap 要正确设置，避免材质发灰、过曝或闪烁。
- 重要角色和近距离武器可使用较高质量阴影；远处环境和大量僵尸使用更节制的阴影/LOD 策略，优先保证稳定 FPS。
- 破败城市中的建筑、车辆、路障、围栏等视觉模型与简化碰撞体分开，碰撞体尽量简单可靠。
- 僵尸如果使用骨骼模型，至少有 Idle / Walk or Run / Attack / Hit / Death 动画，并通过 AnimationMixer 做平滑切换；不同个体可以使用轻微不同的动画起始相位、速度或姿态，避免整群完全同步。
- 僵尸追踪需要速度加减速和转向平滑，不能每帧瞬间面向玩家。攻击动作要与实际伤害触发点对应。
- Boss 使用独立骨架/体型/材质辨识度，技能动画包含前摇、伤害窗口、收招和冷却四个阶段；伤害判定不能比视觉预警更早无缘无故命中玩家。
- 枪口火焰、弹壳、命中粒子、血/受击效果等短寿命对象尽量使用对象池，避免战斗中频繁 GC 导致卡顿。
- 不要在每帧循环大量 `new Vector3()`、`new Raycaster()`、`clone()` 或创建材质；复用临时对象和共享资源。
- 使用 `LoadingManager` 或等价真实资源追踪连接前文 Loading UI，让模型、纹理、音频加载进度是真实的。
- 渲染分辨率要限制 `devicePixelRatio` 上限并结合设备性能档位；高 DPI 手机不能无脑按原生 DPR 全分辨率渲染。
- 根据性能档位调整阴影分辨率、粒子密度、远距离模型细节、后处理等，但枪械瞄准、敌人辨识和碰撞不能因低画质失效。

如果引入新的 Three.js helper、loader 或第三方库，先说明必要性，避免为了一个小效果加入重型依赖。优先保持现有技术栈可维护。

### 22.8 手部/武器动画与多人网络状态必须一致

为避免“本地看见动作、别人看不见”或“动画和真实数据不同步”，将武器动作抽象为明确状态，例如：

`idle / move / sprint / ads / fire / reload / switch / jump / land / dead`

状态名称可根据现有代码调整，但必须有同等语义。关键动作如 fire、reload、switch 使用带时间戳/序号的事件，远端客户端只消费一次。

真正影响游戏结果的事项仍以后端/权威逻辑为准，例如：是否能开火、当前弹匣子弹数、是否完成换弹、玩家是否死亡。动画只表现状态，不能反过来成为可作弊的最终数据来源。

### 22.9 将新要求加入实施顺序，不允许一次性乱改

在原有 Phase 流程中按以下方式吸收本轮新增内容：

- **Phase 2** 同时完善注册/登录/会话 UI，并建立 `tutorialCompleted` 等玩家档案字段与新手教学入口状态。
- **Phase 3** 优先完成第一人称 viewmodel、完整换弹、手部动作、第三人称 player world model、僵尸/Boss 动画细节和性能档位，再继续堆更多视觉特效。
- **Phase 5** 完成真实会话、登录安全、教学进度持久化和服务端教学完成标记。
- **Phase 6** 同步玩家移动、瞄准、武器、射击、换弹、切枪等动画状态/动作事件，让其他玩家实际看到对应动作。

每个阶段先做可运行的纵向切片再扩展，不允许先重写一半工程后长期无法启动。

### 22.10 本轮追加验收标准

除了前文所有验收项，还必须同时满足：

- 新账号第一次进入时无法绕过新手教学直接进入普通单人/多人。
- 新手教学实际可完成，目标约 2–4 分钟，并覆盖移动、视角、跳跃、射击、换弹、ADS、身体命中与爆头反馈。
- 教学中断后能从合理进度继续，完成状态由服务端持久化。
- 第一人称能持续看到质量合格的双手与武器。
- 手枪与 AK-47 都有可见且符合武器结构的换弹流程；换弹动画与真实弹药变化对应。
- 本地玩家有 Idle / Move / Sprint / ADS / Fire / Reload / Switch / Jump / Land 等平滑手部/枪械状态。
- 多人时其他玩家能看到角色持枪、瞄准、开火、换弹、切枪、跑动、跳跃等第三人称动作。
- 远端玩家动画不会因为网络更新而不断瞬移、反复播放或手枪分离。
- 登录刷新后会话能正确恢复；退出后服务端会话真正失效；账号数据不以 localStorage 为权威。
- Three.js 在 30/60/90/120/144 FPS 档位下动画速度保持一致，不使用帧数硬编码动作速度。
- 高 DPI 手机有合理 DPR/画质限制，不因为追求清晰度导致严重掉帧发热。
- 僵尸动画有状态切换与个体差异，Boss 技能动画与实际伤害窗口一致。

### 22.11 本轮额外禁止事项

不要只给换弹按钮加一个进度条而没有真实手部/枪械动作；不要让远端玩家永远是静止 T-Pose 或枪械漂浮；不要每帧同步骨骼；不要把教学做成十几分钟剧情；不要让新玩家通过刷新/改 localStorage 跳过教学；不要为“完善登录”伪造邮件、验证码或第三方 OAuth 功能；不要因为加入高质量 Three.js 细节而破坏移动端性能；不要把 FPS 动画写成“每 60 帧完成一次”的固定帧逻辑。

---

## 23. 本轮追加：主页 3D 沉浸、游戏化 UI、Loading、交互限制与 FOV

本节是在前文所有要求之上追加，不替换、不删除、不弱化任何既有需求。实现时仍然必须先审计现有项目，再做增量升级。

### 23.1 Homepage 必须展示玩家 3D 角色并具有游戏代入感

主页不是普通网页菜单，也不能像传统后台或老式 H5 游戏。登录后主页需要成为一个“可进入的游戏大厅 / Main Lobby Scene”。

- 在 Homepage 中真实展示当前玩家的 **3D 角色/身体模型**，而不是只放头像或静态 PNG。
- 玩家角色要显示当前装备状态，至少能正确表现默认手枪；解锁并装备 AK-47 后，主页角色展示也要同步更新。
- 角色至少有自然的 Idle 呼吸、轻微重心变化、持枪姿态和细微头部/手臂动作，不能像静止模型或 T-Pose。
- 主页角色与武器优先复用游戏内资源和材质体系，避免主页和实战看起来像两套完全不同的游戏。
- 主页允许适度的镜头缓慢移动、景深感、前后景层次、环境粒子、微弱光照变化或鼠标/触摸视差，但效果必须克制，不能造成眩晕，也不能显著增加移动端功耗。
- 主页背景应延续“破败城市”世界观，可使用安全屋、补给点、临时军械区、街区据点等视觉语言。不要使用纯黑背景 + 一堆矩形按钮作为最终主页。
- UI 面板应像现代射击游戏 Lobby/HUD：半透明层、层级、适当模糊/玻璃感、清晰图标、状态反馈和动效；不能做成普通网站导航栏。
- 不要为了“3D”把每个 UI 都做成旋转 3D 文字。重点是空间、角色、灯光、相机和层次带来的游戏感。
- 好友/社交入口可以放在主页边缘区域，显示好友在线/离线、邀请状态等，但不能抢占中心角色和主要菜单的视觉焦点。
- 主页主功能仍保持前文固定结构：开始游戏、军火处、黑市、设置；不要新增大量无关入口。

### 23.2 防止出现普通网页的选择、拖拽和缩放体验

整个产品需要表现为“游戏客户端”，而不是可以随意框选文字、拖图片的网页。

普通游戏 UI / Canvas / HUD / 主页展示区域必须：

- 禁止普通文本被鼠标拖选，高亮选择状态不应出现在菜单/HUD/按钮/标签上。
- 图片、图标、Canvas、模型预览等不能被浏览器当成图片拖拽。
- 禁止图片 ghost drag、长按图片弹出保存菜单等破坏游戏体验的默认行为。
- 游戏区域禁止浏览器默认的双击缩放、手势缩放、页面拖拽/回弹干扰。
- 右键默认菜单在游戏区域内应被抑制，避免战斗时误弹浏览器菜单；如果未来存在开发者模式，可由开发配置开启。
- 禁止因触摸滑动导致页面滚动、橡皮筋回弹或浏览器页面手势打断移动/瞄准。
- `img` 等视觉资源设置为不可拖拽；CSS 游戏壳层使用合适的 `user-select`、`-webkit-user-select`、`-webkit-touch-callout`、`touch-action` 等策略。
- 需要正确设置 viewport，使手机横屏游戏不会因为双击或误触不断放大缩小。
- 不能依赖单纯 `overflow: hidden` 解决全部问题；需要按 Desktop/Touch 输入路径分别处理浏览器默认事件。

注意：不要因此破坏登录系统。用户名/密码输入框、必要的文本输入和密码管理器/自动填充必须仍然可正常工作；上述“不可选择/不可拖拽”主要针对游戏 UI、Canvas、HUD、菜单文字和图片资源，而不是阻止玩家正常输入登录信息。

也不要声称网页可以 100% 阻止用户通过浏览器开发者工具复制资源；这里要求的是消除普通玩家能直接看到和触发的“网页式交互感”。

### 23.3 整体界面流畅度与动画预算

UI 动画、Three.js 和网络逻辑必须共同保证流畅，而不是视觉效果堆满后卡顿。

- 所有菜单切换使用统一的过渡系统，避免突然 display none/block 闪烁。
- 主页面板进入/退出、模式选择、军火处、黑市、设置、好友邀请、结算等需要短而干净的过渡动画。
- 动画优先使用 transform/opacity 等高效属性，避免频繁触发布局重排。
- 不要在每帧创建大量 DOM、Vector、Material、Geometry 或临时对象；Three.js 高频路径尽量复用对象。
- UI 动画不能和游戏主循环互相阻塞；避免同步大任务导致明显卡顿。
- 低性能设备自动降低主页环境粒子、动态阴影、后处理、反射和高成本效果，但保留整体设计风格。
- 页面切换和游戏启动过程中不能出现明显白屏、无响应或按钮按了没有反馈。
- 点击/触摸按钮后立即有视觉反馈，即使后台资源仍在加载也要显示状态。

### 23.4 按钮必须有现代射击游戏质感

所有主要按钮都要有完整状态，而不是普通 HTML 默认按钮：

- Default
- Hover（桌面）
- Focus（键盘/手柄未来兼容）
- Pressed/Active
- Disabled
- Loading
- Selected

按钮要求：

- 视觉统一，边框、材质、光效和动效符合《尸潮乱斗 / Horde Mayhem》的现代枪击游戏方向。
- Hover/Pressed 动效短促、有力量，不使用夸张弹跳或老式发光渐变。
- 触摸设备必须有按下反馈，不能依赖 hover。
- 点击可配合轻量 UI 音效；音效音量受设置控制。
- “开始游戏”等关键按钮可以有更高级的视觉层级，但不要每个按钮都做成完全不同风格。
- Disabled 状态要一眼能看出不可用，同时提供原因提示（例如未登录、未解锁、邀请冷却中）。
- Loading 状态不能让用户重复点击触发多次请求。

### 23.5 Loading 需要真正有游戏动画与真实进度

在前文真实资源 Loading 的要求上进一步强化：

- 首次启动、进入主页、进入战局、多人房间开始战局都需要对应的 Loading 过渡。
- Loading 不是单纯旋转圈。需要有《尸潮乱斗 / Horde Mayhem》Logo/标题、动态背景或简洁的 3D/2D 动画、真实资源进度条、当前加载阶段文本。
- 可以展示破败城市轮廓、感染警告扫描、武器轮廓、弹壳/粒子等轻量主题动效，但不能影响加载性能。
- 实际资源加载使用 LoadingManager 或等价机制聚合纹理、模型、音频等真实进度。
- 网络房间 Loading 要区分“资源加载”和“等待其他玩家/服务器同步”，不能把所有等待都伪装成 99%。
- 进度达到完成后使用短暂淡出/镜头过渡进入场景，避免突然切屏。
- Loading 动画在 30 FPS 设备上同样要平滑，不能依赖固定帧数。
- 若资源加载失败，需要显示可理解的错误、重试和返回操作，而不是卡死在 Loading。

### 23.6 FOV 与镜头系统

FOV 必须作为真正的 FPS 设置和相机系统处理，而不是随意写死。

- 在设置中提供 **FOV / 视野范围** 选项，并在配置层集中管理。
- 建议允许的世界相机 FOV 范围为 **70–110**，默认值使用合理的中间值（例如 85），但最终默认值放在 `config.js`，便于测试后调整。
- 使用滑杆或分档数值显示当前 FOV，修改时在安全场景或预览中即时反馈。
- Desktop 可以开放完整范围；手机/平板若超宽 FOV 导致严重畸变、性能或眩晕问题，可以配置更保守的建议范围，但不要偷偷改掉玩家手动选择而不提示。
- ADS 时不能直接瞬间跳 FOV。需要平滑过渡到更窄的瞄准 FOV，并根据武器配置不同 ADS 倍率。
- 手枪与 AK-47 的 ADS FOV/镜头偏移允许独立配置。
- Sprint、受伤、Boss 冲击等镜头效果只能轻量改变 FOV，必须有限幅、平滑回弹，禁止疯狂 zoom in/out。
- 第一人称武器建议使用独立 **Viewmodel Camera / Viewmodel FOV** 或等价方案，使手和枪不会因为世界 FOV 变化严重拉伸、穿模或遮挡。
- World Camera FOV 与 Weapon/Viewmodel FOV 分离配置；不要直接让枪械模型完全跟随 110° 世界 FOV 变形。
- Resize、横竖屏变化、设备 DPR 改变时正确更新相机 aspect/projectionMatrix。
- FOV 改动不能影响射线命中逻辑、网络同步或玩家真实碰撞体。

### 23.7 主页 3D 镜头与交互边界

- Homepage 3D 场景允许轻量相机视差，但不能让用户误以为主页已经进入第一人称战斗。
- 鼠标移动/手指轻拖可以产生有限的镜头响应；必须设置最大角度和阻尼，不允许无限旋转导致 UI 丢失。
- 打开设置/军火处/黑市时，可让镜头平滑切换到对应构图，例如军火处聚焦武器区域、黑市切到商品展示区域，但整个主页仍属于同一视觉空间。
- 退出子页面时镜头平滑返回主页主构图。
- Camera transition 使用插值/缓动，禁止瞬移式切换。
- 移动端如果检测到性能不足，可关闭视差或降低更新频率。

### 23.8 本轮追加验收标准

除前文所有验收标准外，还必须满足：

- 登录后 Homepage 能看到当前玩家的 3D 角色和当前装备，不是只有静态头像。
- Homepage 有明显 3D 空间、角色、灯光和世界观氛围，但 UI 仍然清晰可用。
- 主页不是“普通网站 + Three.js 背景”，而是完整统一的游戏 Lobby 体验。
- 普通菜单文字无法像网页文章一样随意拖选；图片/图标不会被拖成浏览器 ghost image。
- 手机游戏区域不会因为双击、拖动、长按而频繁触发网页缩放、滚动、图片菜单等默认行为。
- 登录输入、密码管理器和必要输入行为仍然可用。
- 页面、菜单、按钮和 Loading 都有统一、现代且流畅的游戏化动效。
- Loading 使用真实进度；资源失败可重试，不会永久卡死。
- FOV 设置可用，世界相机 FOV 与第一人称枪械显示不会互相造成严重畸变。
- ADS、Sprint 等 FOV 变化平滑、有上限，不会制造强烈眩晕。
- 30/60/90/120/144 FPS 下 UI、Loading、相机和 FOV 动画速度保持一致。

### 23.9 本轮额外禁止事项

不要把 Homepage 做成普通网站布局；不要用默认 HTML 按钮作为最终视觉；不要让文字和图片在游戏 UI 中被随意选中/拖拽；不要靠一个 `user-select:none` 就声称全部移动端交互问题已经解决；不要为了禁止复制而破坏登录输入和密码管理器；不要伪造 Loading 进度；不要让 Loading 动画比真正的资源加载还重；不要把 FOV 写死在多个文件；不要让 ADS 直接瞬间跳变；不要让世界 FOV 把第一人称枪械拉伸得不自然；不要让 Homepage 的 3D 特效导致手机持续高温、掉帧或耗电异常；不要使用老式网页霓虹按钮、过量发光边框和廉价渐变来冒充“游戏感”。
